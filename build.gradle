plugins {
    id 'java'
}

group = 'cl.tenpo'
version = '0.0.1-SNAPSHOT'

java {
    sourceCompatibility = '21'
}

configurations {
    compileOnly {
        extendsFrom annotationProcessor
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation group: 'io.projectreactor', name: 'reactor-core', version: "$reactor"
    implementation 'com.github.javafaker:javafaker:1.0.2'
    compileOnly group: 'org.projectlombok', name: 'lombok', version: "$lombok"
    annotationProcessor "org.projectlombok:lombok:$lombok"
    implementation 'ch.qos.logback:logback-classic:1.4.14' // Para que funcione el logging
    testImplementation group: 'io.projectreactor', name: 'reactor-test', version: "$reactor"

}

build {
    doLast {
        exec {
            executable "./pre-commit-setup.sh"
        }
    }
}

tasks.named('test') {
    useJUnitPlatform()
}

build {
    doLast {
        exec {
            executable "./pre-commit-setup.sh"
        }
    }
}

test {
    useJUnitPlatform()
}